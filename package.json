{
    "name": "calculafrette",
    "version": "0.1.0",
    "private": true,
    "scripts": {
      "start": "cd front && npm start",
      "install": "cd front && npm install",
      "build": "cd front && npm run build",
      "build-docker": "git pull && npm run build && docker-compose up",
      "build-docker-with-reinstall": "git pull && npm i && npm run build && docker-compose up --build --force-recreate",
      "test": "cd front && npm run test",
      "eject": "cd front && npm eject"
    }
  }
  